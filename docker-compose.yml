services:
  python-api:
    build:
      context: ./python
    ports:
      - "8000:8000"

  dashboard:
    build:
      context: ./dashboard
    environment:
      PYTHON_API_URL: http://python-api:8000
    ports:
      - "3000:3000"
    depends_on:
      - python-api
